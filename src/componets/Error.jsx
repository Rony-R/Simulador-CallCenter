import React from "react";

const Error = ({ mensaje }) => (
  <div className="card-panel red lighten-1">
    <p className="whtT">Las entradas deben ser mayores a cero.</p>
  </div>
);

export default Error;
